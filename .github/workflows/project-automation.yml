# SPDX-FileCopyrightText: 2025-2026 SecPal Contributors
# SPDX-License-Identifier: CC0-1.0

---
name: Project Board Automation

on:
  issues:
    types:
      - opened
      - reopened
      - closed
      - labeled
  pull_request:
    types:
      - opened
      - ready_for_review
      - closed
      - converted_to_draft
      - labeled
  pull_request_review:
    types:
      - submitted

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  project-automation:
    name: Project automation
    # main
    uses: SecPal/.github/.github/workflows/project-automation-core.yml@da7d5f19a1ff4e65bfdbe7ad0a66e13f6172ada9
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}

  draft-reminder:
    name: Draft PR Reminder
    # main
    uses: SecPal/.github/.github/workflows/draft-pr-reminder.yml@da7d5f19a1ff4e65bfdbe7ad0a66e13f6172ada9
    permissions:
      issues: write
      pull-requests: write
