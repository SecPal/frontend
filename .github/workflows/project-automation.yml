# SPDX-FileCopyrightText: 2025-2026 SecPal Contributors
# SPDX-License-Identifier: CC0-1.0

---
name: Project Board Automation

on:
  issues:
    types:
      - opened
      - reopened
      - closed
      - labeled
  pull_request:
    types:
      - opened
      - ready_for_review
      - closed
      - converted_to_draft
      - labeled
  pull_request_review:
    types:
      - submitted

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  project-automation:
    name: Project automation
    # main
    uses: SecPal/.github/.github/workflows/project-automation-core.yml@dd8139ced2efb02c20ff7302f44907b5b62719b2
    secrets:
      APP_ID: ${{ secrets.APP_ID }}
      APP_PRIVATE_KEY: ${{ secrets.APP_PRIVATE_KEY }}

  draft-reminder:
    name: Draft PR Reminder
    # main
    uses: SecPal/.github/.github/workflows/draft-pr-reminder.yml@dd8139ced2efb02c20ff7302f44907b5b62719b2
    permissions:
      issues: write
      pull-requests: write
